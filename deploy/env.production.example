# --- Django ---
DJANGO_ENV=prod
DEBUG=False
SECRET_KEY=CHANGE_ME_generate_with_python_c_import_secrets_print_secrets_token_hex_32
JWT_SIGNING_KEY=CHANGE_ME_different_from_SECRET_KEY

# --- Database ---
# SQLite for the smallest deployments — switch to PostgreSQL when scaling.
DATABASE_URL=sqlite:///db.sqlite3
# For PostgreSQL: DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/DBNAME

# --- Hosts ---
ALLOWED_HOSTS=YOUR_DOMAIN
CORS_ALLOWED_ORIGINS=https://YOUR_DOMAIN
CSRF_TRUSTED_ORIGINS=https://YOUR_DOMAIN
FRONTEND_BASE_URL=https://YOUR_DOMAIN

# --- Email ---
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=CHANGE_ME
EMAIL_HOST_PASSWORD=CHANGE_ME
DEFAULT_FROM_EMAIL=noreply@YOUR_DOMAIN

# --- Celery (Redis broker; uses starter-celery + starter-celery-beat services) ---
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
CELERY_TASK_DEFAULT_QUEUE=celery
# Opt-out for very small deployments without Redis. When True, tasks run
# synchronously in the request thread and the Celery services are not needed.
#CELERY_TASK_ALWAYS_EAGER=True

# --- Upload ---
MEDIA_ROOT_DIR=media
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760
MAX_UPLOAD_FILE_SIZE=10485760

# --- API ---
API_PAGE_SIZE=20
PASSWORD_RESET_FRONTEND_PATH_PREFIX=/user/reset-password
